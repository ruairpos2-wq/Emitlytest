# Парсер новостей про Сбербанк

Проект для парсинга новостей про Сбербанк из двух источников:
1. **РБК** (https://www.rbc.ru/) - статьи за последние 60 дней
2. **Telegram** (@markettwits) - до 200 сообщений с упоминанием Сбербанка

## 📋 Установка

### 1. Установите зависимости Python

```bash
pip3 install -r requirements.txt
```

### 2. Установите Chrome и ChromeDriver (для Telegram парсера)

**macOS:**
```bash
brew install --cask google-chrome
brew install chromedriver
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt-get install chromium-browser chromium-chromedriver

# Arch Linux
sudo pacman -S chromium
```

**Windows:**
1. Скачайте Chrome: https://www.google.com/chrome/
2. Скачайте ChromeDriver: https://chromedriver.chromium.org/downloads
3. Добавьте ChromeDriver в PATH

## 🚀 Запуск

### Полный парсинг (РБК + Telegram)

```bash
python3 main.py
```

### Только РБК

```bash
python3 rbc_parser.py
```

### Только Telegram

```bash
python3 telegram_selenium_parser.py
```

## 📁 Результаты

После выполнения создается папка `results/` с файлами:

- `rbc_results.json` - статьи с РБК
- `telegram_results.json` - посты из Telegram

### Формат РБК:
```json
[
  {
    "title": "Заголовок статьи",
    "url": "https://www.rbc.ru/...",
    "date": "2025-11-13"
  }
]
```

### Формат Telegram:
```json
[
  {
    "text": "Текст сообщения...",
    "date": "2025-11-13",
    "post_url": "https://t.me/markettwits/1234"
  }
]
```

## ⚙️ Настройка

### RBC Parser (rbc_parser.py)

- `KEYWORDS` - ключевые слова для поиска (по умолчанию: сбербанк, сбер)
- `cutoff_date` - период поиска (по умолчанию: 60 дней)
- `max_pages` - количество страниц для парсинга (по умолчанию: 5)

### Telegram Parser (telegram_selenium_parser.py)

- `channel_name` - имя канала (по умолчанию: 'markettwits')
- `target_messages` - количество сообщений для загрузки (по умолчанию: 200)
- `headless` - запуск браузера в фоновом режиме (True/False)

## 🔧 Структура проекта

```
profi/
├── main.py                         # Главный скрипт
├── rbc_parser.py                   # Парсер РБК
├── telegram_selenium_parser.py     # Парсер Telegram с Selenium
├── requirements.txt                # Зависимости
├── README_PARSERS.md              # Эта инструкция
└── results/                        # Результаты парсинга
    ├── rbc_results.json
    └── telegram_results.json
```

## 📝 Примечания

1. **Anti-ban**: Парсер автоматически добавляет задержки между запросами
2. **Selenium**: Требует установленного Chrome/Chromium
3. **РБК**: Использует RSS-ленты + HTML-парсинг для максимального охвата
4. **Telegram**: Эмулирует прокрутку страницы для загрузки старых постов

## ❗ Troubleshooting

### Ошибка "ChromeDriver not found"

**macOS:**
```bash
xattr -d com.apple.quarantine /opt/homebrew/bin/chromedriver
```

**Linux/Windows:**
Убедитесь, что ChromeDriver в PATH

### Ошибка "SSL Certificate"

Добавьте в код:
```python
import ssl
ssl._create_default_https_context = ssl._create_unverified_context
```

### РБК возвращает 0 результатов

Это нормально, если за последние 60 дней не было статей про Сбербанк.
Измените `cutoff_date` в `rbc_parser.py` для большего периода.

## 📊 Пример вывода

```
======================================================================
ПАРСИНГ НОВОСТЕЙ ПРО СБЕРБАНК
======================================================================

🔵 ШАГ 1: ПАРСИНГ РБК
----------------------------------------------------------------------
[1/2] Парсинг RSS-лент РБК...
  ✓ [2025-11-13] Греф спрогнозировал новый рекорд по размеру дивидендов
  → Найдено в RSS: 5 статей

======================================================================
📊 ИТОГОВАЯ СТАТИСТИКА
======================================================================
✅ Найдено 5 статей на РБК
✅ Найдено 12 постов в Telegram
📁 Файлы сохранены в ./results/
======================================================================
```

## 📄 Лицензия

MIT
